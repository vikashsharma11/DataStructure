
class Solution:
    #Complete the below function
    def search(self,arr, N, X):
        
        for i in range(len(arr)):
            if arr[i]==X:
                return arr.index(i)
            
        return -1
